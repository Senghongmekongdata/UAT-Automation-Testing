[
  {
    "name": "DIM_SAMPLE_ACCOUNT",
    "source_query": "SELECT ID, SPLIT_HASH, ACCOUNT_TYPE, ACCOUNT_NUMBER, CURRENCY, STATUS FROM MAIN.ACC_ACCOUNT",
    "dest_query": "SELECT ID, SPLIT_HASH, ACCOUNT_TYPE, ACCOUNT_NUMBER, CURRENCY, STATUS FROM ETL_USER.DIM_SVBO_ACCOUNT",
    "primary_key": ["id"]
  },
  {
    "name": "DIM_SAMPLE_CARD",
    "source_query": "SELECT ID, CARD_HASH, CARD_MASK, CARD_TYPE_ID, STATUS FROM MAIN.ISS_CARD",
    "dest_query": "SELECT ID, CARD_HASH, CARD_MASK, CARD_TYPE_ID, STATUS FROM ETL_USER.DIM_SVBO_CARD",
    "primary_key": ["id"]
  }
]
